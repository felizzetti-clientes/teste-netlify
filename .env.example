# Nenhuma variável de ambiente é necessária para rodar o projeto hoje.
# Este arquivo é um modelo para quando alguma integração precisar de chave.
# Nunca coloque valores reais aqui nem em .env / .env.local — apenas em
# variáveis de ambiente configuradas direto no Netlify.

# Exemplo (não usado ainda): endpoint de um serviço de formulário
# NEXT_PUBLIC_FORM_ENDPOINT=
